# Dockerfile для контейнера OCR Pipeline
# Этот контейнер устанавливает зависимости в /deps-source
# Init контейнер копирует их в volume при первом запуске

FROM python:3.11-slim

# Установка системных зависимостей
# Необходимы для PyMuPDF и opencv-python
RUN apt-get update && apt-get install -y \
    libgl1-mesa-glx \
    libglib2.0-0 \
    libgomp1 \
    && rm -rf /var/lib/apt/lists/*

# Копирование requirements.txt
COPY requirements.txt /tmp/requirements.txt

# Установка Python зависимостей в /deps-source (исходная директория)
RUN mkdir -p /deps-source && \
    pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir --target=/deps-source -r /tmp/requirements.txt

# Проверка установки
RUN PYTHONPATH=/deps-source python -c "import fitz; import docx; import PIL; import cv2; import numpy; import langchain_openai; import pydantic; print('All dependencies installed successfully')"

# Держим контейнер запущенным
CMD ["tail", "-f", "/dev/null"]

